@model IEnumerable<KVA.Cinema.Models.ViewModels.Tag.TagDisplayViewModel>
@using KVA.Cinema.Models.ViewModels.Tag;

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<h1>Index</h1>

@if (User.Identity.IsAuthenticated)
{
    <p>
        @Html.ActionLink("Create New", "Create", "Tags", null, new { @class = "btn btn-primary" })
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.ActionLink(
                    ViewBag.SortingField == TagSort.Text
                    ? $"Text {(ViewBag.SortDescending ? "▾" : "▴")}"
                    : "Text",
                    "Index", new { sortingField = TagSort.Text, isSortDescending = (ViewBag.SortingField == TagSort.Text && !ViewBag.SortDescending) })
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>
            @if (User.Identity.IsAuthenticated)
            {
                <th><!--Header for Action links--></th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item, "_TagColor")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>

                @if (User.Identity.IsAuthenticated)
                {
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { Id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { Id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { Id = item.Id })
                    </td>
                }
            </tr>
        }
    </tbody>
</table>

@section Scripts
{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
